# archive/

Retired duplicates kept for history (not imported by any live code).

- `legacy_api/` — 2026-05 copy of the API layer, superseded by `prism/core/{api_client,llm_api}.py` and `config/api/`. The old `tests/test_api_config_env.py::test_legacy_*` parity check was removed when this was retired.
