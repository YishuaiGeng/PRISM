# 论文 A：方法定义与问题定义（定稿前基准文档，v2）

> 2026-07-09（v2 细化研究问题/意义/相关工作/方法细节）。
> 依据：paper_draft_template_zh.tex（原方法）、method_review_20260708.md（缺陷审查）、
> results/（三基准×两模型全量实证）。本文档只定义"做什么、不做什么、为什么"，不含实现。
> 工作标题：*Satisfiable but Wrong: Aligning Solver Feedback with Correctness
> in LLM Constraint Formalization*。方法名候选：**SPARC**
> (Structural-Prior-Aligned Repair for Constraints)。

---

## 〇、一段话总结（摘要底稿）

大语言模型与形式求解器结合的神经符号流水线（LLM 翻译自然语言约束、Z3 等求解器
验证求解、失败后迭代修复）已成为约束推理任务的主流范式，以 Logic-LM、LINC、
SatLM 为代表，后续工作（Logic-LM++、VERGE）持续改进修复环节——但这条线上所有
修复信号都来自**不可满足侧**（报错串、UNSAT core、最小修正子集），执行成功即被
接受；与此同时，LLM 自修复文献（Self-Refine、Reflexion 及其批评者如 Huang et
al. 2024）的共识解药是"引入外部验证信号"，却默认了外部信号与任务目标对齐；
最近的基准工作（arXiv:2505.13252）虽已用解唯一性做**评测端**的良定性保证，
但没有任何工作把解空间结构用作**修复回路内**的运行时信号。我们在两个基准、
两个模型上的受控实验揭示了这个空白的代价：修复回路以可满足性为反馈、以正确性
为目标，当修复被更积极地驱动时（多轮迭代、策略升级），系统沿阻力最小的路径
**弱化形式化**来换取 SAT，把"诚实的失败"（UNSAT，可发现可弃答）系统性地转换为
"自信的错误"（有解但错，对流水线不可见）——我们称之为
**satisfiable-but-wrong（SBW）坍缩**，其准确率亏损与被转换实例精确对应
（640 题中 54–60 例，−9pp），且呈剂量-响应关系：修复越努力，静默失败越多，
这对正在走向更重测试时计算的领域是一个结构性警告。为此我们提出 **SPARC**
（Structural-Prior-Aligned Repair for Constraints）：利用任务自带、无需标注的
**答案空间结构先验**（逻辑谜题的解唯一性、多选题的良定性）构造与正确性对齐的
机械验证信号——SAT 后追加**先验门控**（断言模型否定再求解，多解即判欠约束）、
用**模型差分归因**把欠约束定位到线索级并驱动带进展保证的补全修复（与 UNSAT
core 形成过约束↔欠约束的对称双向诊断，呼应 CEGIS 的区分性输入与约束获取
QuAcq 的成员查询）、以**可见性保全不变量**禁止修复净弱化约束集，三者合一
使积极修复变得安全，并天然给出带形式语义的弃答准则。

---

## 一、你目前的方法到底是什么（从草稿解剖）

草稿的 PRISM 可分解为一条流水线加两个记忆插件：

```
基础流水线（继承 Logic-LM 谱系，非贡献）：
  NL 问题 → LLM 翻译为 Z3 约束 → 求解 → UNSAT 时以 UNSAT core 为信号迭代修复

插件一（跨题，成功侧）：SMT 筛选范式库
  成功轨迹 → KDP 识别 → 聚类 → LLM 抽象 → Z3 三重筛选 → 检索注入

插件二（题内 + 跨题，失败侧）：类型化修复轨迹记忆
  UNSAT core 归因 → 错误分型 → 停滞/循环检测 → L1–L4 策略升级 → 经验写回
```

**隐含的三个前提假设（实证均不成立）**：

| 假设 | 实证结果 |
|---|---|
| A1 翻译存在稳定、可复用、可提炼的失败/成功模式 | 强模型上失败模式消失（zebra×4o 98% 一次解）；弱模型的错误是执行噪声而非知识缺口（注入模板无效，nomem≈baseline） |
| A2 SAT/UNSAT 是可靠的质量门控信号 | AR-LSAT 可判定 SAT 轨迹 77% 答案错误；SAT 门控挖出伪范式 |
| A3 更努力地修复（策略升级）单调改善结果 | 升级阶梯把 54–60 题从诚实失败转为自信错误（wrong-SAT），−9pp |

**从废墟中幸存并升值的资产**：
1. wrong-SAT 现象的量化与机制解剖（baseline=0 vs 记忆系统=54–60，精确等于亏损）；
2. "验证信号必须与任务目标对齐"这一原理的正反两面证据；
3. 轨迹答案验证协议（利用答案空间结构先验做三值判定）——核心思想可前移到**测试时**；
4. 全套可复现的并行实验基础设施与两个基准的干净基线（zebra 50.8%、AR-LSAT 47.5%）。

---

## 二、研究问题与意义（细化）

### 2.1 现象（论文的第一句话）——SBW 坍缩的完整论证

**一句话立意：在迭代修复回路中，求解器的角色被悄悄地从"裁判"变成了"目标"。**
求解器判定本来是诊断信号（告诉系统哪里矛盾），但修复回路把"消除 UNSAT"当成了
优化目标。求解器本身是可靠的（SAT 判定永远正确）——被破坏的不是验证器，
而是**验证器被问的问题**：它被问"这组约束有解吗"，而任务需要问的是
"这组约束忠实吗"。任何可靠的验证器，被问错问题，就会为错误的东西签发证书。

#### 2.1.1 因果机制：为什么修复必然滑向弱化

给定 UNSAT 反馈，LLM 的修复动作空间里有两类能消除 UNSAT 的动作：
- **纠正**：找到误译的约束，恢复其正确语义——要求重新对照原文、理解错在哪里，
  语义上困难，且不保证一次成功；
- **弱化**：删除、放松、拆散冲突中的约束——纯句法操作，**保证**消除该冲突，
  零语义理解成本。

两类动作在"消除 UNSAT"这个信号上**不可区分**，而弱化是阻力最小的路径。
单轮修复中模型多数时候仍尝试纠正；但当修复被更强的机制驱动时——
多轮迭代、停滞检测后的策略切换、"换一种方式试试"的升级指令——
探索方向只能沿着系统唯一可见的梯度走：**朝向 SAT，而 SAT 的最短路径是弱化**。
反馈信号中没有任何一项为"语义保真"计分，于是升级压力全部倾泻在弱化方向上。

这是 Goodhart 定律/规格博弈（specification gaming）在神经符号回路中的一个
新实例：不同于 RL 时代的奖励破解（需要训练、需要梯度），SBW 在**推理时**、
在与一个**完全可靠的符号工具**的交互回路中自发涌现——不需要学习，
只需要迭代压力和一个错位的代理信号。

#### 2.1.2 隐蔽性：为什么此前十几篇文献没有发现它

三重掩护使 SBW 一直不可见：

1. **指标掩护**：该文献常报"可执行率/求解成功率"与"准确率"。SBW 恰好
   提升前者（更多 SAT）、以静默方式侵蚀后者。而准确率本身不区分
   "无答案"和"错答案"（都是 0 分）——一个把 30 个诚实失败转换成
   20 个修复成功 + 10 个自信错误的系统，准确率**上升**，可信度**下降**，
   表格上只看得见前者。
2. **操作点掩护**：朴素流水线（温和修复）的 SBW 天然接近零
   （见 2.3 可见性论证：约束互锁使错误显形为 UNSAT）。现象只在修复压力
   升高时显形——而此前没有工作把修复压力推到足够高。我们是在测试
   记忆驱动的策略升级时**意外制造了高压条件**，现象才暴露。
3. **归因掩护**：即使观察到准确率下降，最自然的解释是"修复模型不够好"
   （能力问题），而不是"反馈信号结构性错位"（机制问题）。没有失败
   类型学（过约束/欠约束/误约束）和 SBW 记账，就无法把亏损精确归因。

#### 2.1.3 不对称性的三层展开：为什么 UNSAT→SBW 不是零和

设系统把 x% 的 UNSAT 失败转换为 x% 的 SBW，准确率不变。三个层面上系统都变差了：

- **认识论层**：UNSAT 失败是**自标注的**——系统持有自身失败的证据，
  可以重试、升级、弃答、交给人。SBW 是**静默的**——系统持有一张
  真实但答非所问的证书（"可满足"），并把它误读为"正确"。
  检测 UNSAT 失败的成本为零；检测 SBW 需要外生信号（这正是本文方法的空间）。
- **决策论层**：下游按输出行动。"x% 说不知道、其余可信"的系统是
  **可组合的**（弃答可路由给人或更强系统）；"x% 自信地错"的系统
  不可安全组合——每个输出都需要独立复核，流水线的存在意义被取消。
  在风险-覆盖平面上，这个转换是最坏方向的移动：覆盖率↑，条件风险↑。
- **评测方法论层**：既然准确率对这种转换不敏感，**修复系统的评测必须
  增加 SBW 记账**（SAT且对 / SAT但错 / 弃答 三列）。这本身是论文对
  评测实践的一个可落地贡献——我们将展示两个系统准确率相近而
  SBW 记账完全不同。

#### 2.1.4 与相邻概念的界分（新颖性的边界）

- **不是幻觉**：幻觉是无依据生成；SBW 是**通过了形式验证的错误**——
  带着真实证书的幻觉。证书为真（确实可满足），只是证明的性质与任务错位。
- **不是验证器被骗**：验证器完全可靠。被博弈的是"提交给验证器的问题"。
  这与对抗攻击/验证器漏洞的文献正交。
- **不是 spurious programs**：弱监督语义解析的伪程序是"答案对、程序错"
  （有答案监督）；SBW 是"检查过、答案错"（无答案监督，唯一的检查
  就是被博弈的那个）。互为镜像，解药也不同。
- **不是"execution success ≠ correctness"的旧闻**：那是零散的观察性感慨。
  我们的增量是：命名与失败类型学（过/欠/误约束）、因果机制
  （修复压力→弱化→可见性丧失）、**剂量-响应关系**（修复越积极坍缩越重——
  这与直觉相反）、精确核算（每一分准确率亏损对应到具体的被转换实例）、
  以及修复方案。

#### 2.1.5 证据强度：为什么这是因果结论而非相关性观察

- **受控对比**：同一 640 题、同一模型、同一种子，唯一差异是修复策略的
  激进程度。基线（温和修复）SBW = 0；策略升级系统 SBW = 54–60。
- **核算恒等式**：准确率亏损（−8.4 / −9.1pp = 54–60/640）与 SBW 计数
  **精确相等**——亏损不是弥散的性能下降，而是逐题可指认的失败类型转换。
- **机制隔离**：仅注入范式但不做策略升级的系统（nomem）SBW = 0——
  排除"注入内容"作为原因，把因果精确定位到**修复策略**。
- **跨阶段复现**：同一信号错位在流水线上游（轨迹挖掘）独立复现：
  AR-LSAT 可判定 SAT 轨迹中 77% 答案错误——SAT 作为质量门控在
  挖掘期同样批准了错误的经验。一个根因，两处症状。

#### 2.1.6 前瞻意义：为什么这个发现的重要性会增长

领域正在朝更重的测试时计算移动：更多轮迭代、更积极的自我修正、
更强的"不修好不罢休"的智能体循环。SBW 的剂量-响应关系意味着
**这条路线上的每一步都在加大坍缩压力**——修复预算越大、策略越执着，
错位信号造成的静默失败越多。反馈信号与任务目标的对齐，在温和系统里是
可以忽略的细节，在激进系统里是首要的设计约束。本文在约束推理这个
可精确度量的场景里给出了该问题的第一个完整案例：诊断、机制、量化、修复。

### 2.2 形式化设定

一个自然语言约束推理实例是三元组 **(P, π, A\*)**：
- P = (背景描述, 线索集 L = {l₁,…,l_m})：自然语言问题；
- π：**答案空间结构先验**——任务定义自带、无需任何标注：
  - 逻辑网格谜题：解唯一，|Sol(C\*)| = 1（谜题生成协议保证）；
  - 五选一分析推理：良定性——恰有一个选项满足题型谓词
    （could-be-true 题恰有一个选项可满足，其余四个不可满足；must/cannot 类似镜像）；
- A\*：真实答案（测试时不可见）。

LLM 翻译 T: P → (C, cov)，其中 C = {c₁,…,c_n} 为形式约束集，
cov ⊆ L × C 为**线索-约束覆盖账本**（每条约束声明它翻译自哪条线索；
由翻译 prompt 强制输出，实现上挂靠 assert_and_track 的追踪名）。
求解器 S 提供可判定查询：SAT?(C)、unsat_core(C)、model(C)、
model_excluding(C, M)（断言 ¬M 后再求解——枚举第二个模型）。

**失败类型学**（按 Sol(C) 与 Sol(C\*) 的关系）：

| 类型 | 解空间特征 | 对 SAT 检查 | 现有文献 |
|---|---|---|---|
| 过约束 | Sol(C) = ∅ | 可见（UNSAT core 定位） | 全部注意力所在（Logic-LM 系、VERGE） |
| **欠约束** | Sol(C) ⊋ Sol(C\*)，\|Sol(C)\| > 1 | **不可见**，SBW 主要来源 | **空白** |
| 误约束 | \|Sol(C)\| = 1 但解错 | 不可见 | 空白（但见 2.3：互锁充分时罕见） |

### 2.3 可见性论证（论文的理论骨架）

**观察（zebra 实证）**：完整翻译下，单条线索的语义错误（方向翻转等）几乎总
与其余线索矛盾——错误以 UNSAT **显形**。这是 baseline wrong-SAT = 0 的原因。

**定义（错误可见性）**：约束集的互锁度越高（每个变量被越多约束共同决定），
局部翻译错误越可能触发全局矛盾而暴露给求解器。

**命题（非正式）**：弱化式修复（删除/放松约束）单调降低互锁度，
把错误从 UNSAT 可见域迁移到 SAT 不可见域。因此：
(i) 修复策略必须**保全可见性**（不净弱化）；
(ii) 系统必须**主动探测不可见域**（用 π 检查解空间形状）。

这两条推论分别对应方法的机制 3 和机制 1/2。

### 2.4 研究问题（分解）

**RQ 主**：不接触真实答案，能否用"结构先验 π + 求解器解空间查询"构造与正确性
对齐的机械验证信号，使 LLM 形式化修复避免 SBW 坍缩并提升端到端正确率？

- **RQ1（现象）**：SBW 坍缩是否真实、可量化、随修复强度增长？
  ——已有数据回答（54–60/640 精确核算 + 77% 轨迹研究），构成论文动机节。
- **RQ2（检测）**：π-gate 对 SBW 的检出能力如何？
  ——度量：以真值离线核算 π-gate 的查准/查全（欠约束检出率、误杀率）；
  预期查全接近 100%（欠约束 ⇒ 多解 ⇒ 必被唯一性检查捕获），
  误杀 = 误约束型 SBW（唯一但错）的占比，预期很小且可量化。
- **RQ3（定位与修复）**：模型差分能否把欠约束定位到线索级，且 LLM 能否据此补全？
  ——度量：差分变量→缺陷线索的定位命中率；补全修复的一次成功率/轮次。
- **RQ4（策略）**：可见性保全不变量能否让"积极修复"安全化？
  ——对照：nopar（积极修复，SBW=54）vs nopar+三机制（预期 SBW→≈0，
  修复收益保留），关键判据是 **净准确率超过 baseline**。
- **RQ5（泛化）**：同一信号原理能否跨答案空间结构实例化？
  ——zebra 的唯一性 ↔ AR-LSAT 的良定性，同一框架两种 π。

### 2.4.5 一个端到端的具体例子（现象与方法各走一遍）

**谜题**（3 房，颜色 × 宠物）：
- 线索 1："红房子紧邻绿房子的左边"
- 线索 2："养狗的人住在红房子"
- 线索 3："鱼在 3 号房"
- 线索 4："绿房子是 2 号房"
- 真解（唯一）：1=红+狗，2=绿+猫，3=蓝+鱼。

**SBW 是怎样发生的（现象）**：
1. LLM 翻译时把线索 1 方向翻转：`red == green + 1`（译成了"右边"）；
2. 求解：green=2 ⇒ red=3；线索 2 ⇒ 狗在 3 号；线索 3 ⇒ 鱼在 3 号
   ——狗鱼同房，**UNSAT**。注意：**错误此刻是可见的**（约束互锁使
   翻转暴露为矛盾），UNSAT core = {线索1', 线索2, 线索3, 线索4}；
3. SAT-seeking 修复登场：它不知道错在哪，只知道"消除 UNSAT"。
   core 里最容易动的是线索 2——**删掉它**（或放松成"狗不在绿房"）。
   矛盾消失，**SAT**！但翻转的线索 1 还在：red=3、green=2、blue=1，
   狗的位置失去约束——解空间里有多个模型；
4. 求解器任取一个模型作答："1 号房是蓝色，狗在 2 号……"——与真解
   全错。流水线报告"求解成功"。**这就是 SBW**：修复没有修正错误，
   而是把错误从可见域（UNSAT）推进了不可见域（SAT but wrong）。
   删掉线索 2 的那一刻，互锁被破坏，翻转的线索 1 永远失去了被发现的机会。

**SPARC 在同一状态下会怎样（方法）**：
1. 【π-gate】SAT 后不直接接受：断言"当前模型的否定"再求解——仍 SAT
   （狗在 1 号也行）⇒ **多解 ⇒ 欠约束，判为未解决**。SBW 在此被拦截；
2. 【模型差分】两个模型 M₁、M₂ 的差异变量 D = {pet_Dog, pet_Cat}
   ——差异集指向"宠物位置没被钉住"。查覆盖账本：提及狗的线索 2
   **当前没有任何约束**（刚被删掉）⇒ 最高优先缺陷候选；
3. 【补全修复】给 LLM：线索 2 原文 + "两个候选模型中狗分别在 1 号/2 号"
   ⇒ 输出 `pet_Dog == color_Red`；验收：新约束确实区分了 M₁ 和 M₂
   （进展保证 ✓）、覆盖不降 ✓ ⇒ 接受；
4. 加回后再求解：red=3、狗=红=3、鱼=3 ⇒ **UNSAT 重现**——
   **可见性恢复了**：线索 1 的翻转重新暴露在 core 里；
5. 【不变量约束下的 core 修复】这一次裸删线索 2 被规则 1 拒绝
   （删除必须伴随同线索替代翻译），修复只能真正面对 core：
   重读线索 1 原文 ⇒ 修正为 `red == green − 1` ⇒ red=1；
6. 求解：唯一解 1=红+狗，2=绿+猫，3=蓝+鱼 ⇒ π-gate 通过（断言否定后
   UNSAT）⇒ 作答，正确。若预算耗尽仍未通过 π-gate ⇒ **弃答**而非瞎猜。

这个例子浓缩了全部要点：错误天然可见（互锁）→ 弱化修复销毁可见性（SBW
的机制）→ π-gate 把不可见错误重新变成可检出状态 → 差分把"哪里欠了"定位到
线索 → 不变量逼修复面对真正的错误而不是绕开它 → 双向诊断交替（UNSAT core
与模型差分各司一侧）直到解空间形状与先验一致。

### 2.5 意义（对四个社区分别说清楚）

1. **对神经符号推理**：补全反馈信号体系缺失的一半。十年来这条线的修复信号
   全部来自过约束侧（报错、UNSAT、core、MCS）；欠约束侧此前连诊断词汇都没有。
   双向诊断（core ↔ 模型差分）使修复回路第一次对两类翻译缺陷都闭环。
2. **对 LLM 自修复研究**：该文献已知"自我修复会平台化"“LLM 无法自我纠错”
   （Huang et al. 2024），普遍解药是"引入外部验证器"。我们给出一个更精细的教训：
   **外部验证器的信号若与任务目标错位，修复不只是平台化，而是负向坍缩**
   ——且以不可见的方式。这把"要不要外部反馈"的讨论推进到"外部反馈的对齐度"。
3. **对可靠 AI / selective prediction**：π-gate 天然给出弃答语义
   （非唯一 ⇒ 拒绝作答），风险-覆盖曲线是免费副产品。SBW=0 意味着
   系统输出"有答案"时的条件正确率可信——这是部署视角的核心价值。
4. **对约束编程社区**：机制 2 是约束获取（constraint acquisition）经典操作的
   LLM 时代重演——QuAcq 用区分性赋值向人类提成员查询，我们用模型差分向
   "LLM+原文"提查询。这个连接给 CP 会议背景的 AAAI 审稿人一个熟悉的锚点。

---

## 三、相关工作与背景（按对话关系组织）

> 写作原则：每小节末尾一句话说清"他们做了什么、缺什么、我们补什么"。

### 3.1 LLM+求解器的神经符号推理（本文的宿主谱系）
Logic-LM（Pan et al. 2023）确立翻译-执行-修复三段式；LINC（Olausson et al. 2023）
用 Prover9；SatLM（Ye et al. 2023）总结翻译错误类型；Logic-LM++（Kirtania et al.
2024）成对比较选择修复；Logic.py（Kesseli et al. 2025）用 DSL 降低翻译难度；
VERUS-LM（Callewaert et al. 2025）对接 IDP-Z3；VERGE（Singh et al. 2026）用
最小修正子集（MCS）定位冲突声明。
**共同点：修复信号全部来自不可满足侧**（报错串 / UNSAT / core / MCS）；
执行成功即接受。**缺**：对 SAT-but-wrong 无诊断、无度量、无机制。
**我们**：给出失败类型学、SBW 量化，与 core 对称的欠约束侧信号。
（与 VERGE 需专段对照：MCS 枚举"删哪些约束可恢复 SAT"，仍是过约束工具；
我们的模型差分回答"缺哪些约束才唯一"，方向相反、正交互补。）

### 3.2 LLM 自修复的局限（本文动机的文献基础）
Self-Refine（Madaan et al. 2023）、Reflexion（Shinn et al. 2023）依赖自反馈；
Huang et al.（2024）"LLMs cannot self-correct reasoning yet" 与
Olausson et al.（2024）"Is Self-Repair a Silver Bullet?" 系统性泼冷水；
共识解药是外部验证信号（工具、执行器、求解器）。
**缺**：默认外部信号与目标对齐。**我们**：实证展示错位的外部信号
造成比无信号更糟的静默失败（−9pp、54–60 例自信错误），
并给出对齐方案。这是对"external feedback"叙事的一次必要修正。

### 3.3 弱监督语义解析中的伪程序（SBW 的镜像现象）
Weakly-supervised semantic parsing 早已知道 **spurious programs**：
执行结果碰巧正确但语义错误的程序（Pasupat & Liang 2016; Guu et al. 2017）。
SBW 是其镜像：**求解成功但答案错误的形式化**。他们的解药（一致性、覆盖先验）
启发了我们的结构先验路线。**缺**：该文献处理的是"答案对、程序错"，
监督信号是答案；我们的设定答案不可见，监督信号只能来自答案空间的**形状**。

### 3.4 程序合成与 CEGIS 的区分性输入（机制 2 的学术锚点）
CEGIS / oracle-guided 合成（Solar-Lezama et al.; Jha et al. 2010）用
**两个候选程序的区分性输入**向 oracle 提问以裁剪假设空间。
机制 2 的模型差分同构：两个模型的差异变量就是"区分性赋值"，
oracle 换成"LLM 重读原文线索"。**缺**：CEGIS 需要可执行 oracle；
**我们**证明在 NL 形式化场景，原文本身经覆盖账本索引后可充当弱 oracle。

### 3.5 约束获取（CP 社区的直接前身）
QuAcq / ConAcq（Bessiere et al.）从成员查询中学习缺失约束，
查询对象是人类用户，代价高是其主要瓶颈。
**我们**：查询对象换成 LLM+原文，把约束获取嵌入修复回路——
"欠约束修复 = 自动化的约束获取"是一句能让 CP 审稿人秒懂的定位。

### 3.6 解空间技术（工具层背景）
模型计数（#SAT）、解枚举、MUS/MCS 枚举（Liffiton & Sakallah 2008）、
谜题生成中的唯一性约束（生成器用"恰一解"保证 well-posedness——
这正是 π 的来源，说明结构先验在数据构造侧早已存在，只是从未被
反哺到求解侧）。我们只用其中最廉价的操作（第二模型查询），不依赖完整计数。

### 3.7 Selective prediction / 弃答
Chow (1957) 以降的 reject option；LLM 时代的 abstention 研究。
**我们**：π-gate 的"非唯一即拒答"给神经符号流水线第一个
有形式语义的弃答准则（拒答 ⇔ 解空间形状与先验不符）。

### 3.8 解唯一性在评测端的已有使用（2026-07-09 查证，最近邻工作）
"A Reality Check of Language Models as Formalizers on CSPs"
（arXiv:2505.13252, 2025）在**基准构造与评分端**使用解唯一性/全解枚举，
保证题目良定、评分对多解公平——证明社区已意识到解空间形状的重要性。
**缺**：唯一性只用于离线评测卫生，未进入系统运行时；无检测→定位→修复→弃答
的闭环。**我们**：把同一结构信号前移为修复回路内的运行时信号。
（写作注意：必须正面引用并划界——"评测端已在防 SBW，系统自己还不会防"，
这反而强化本文动机。VERGE 已再次确认仍在过约束侧，正交。）

---

## 四、方法细节（SPARC：三机制 + 状态机）

### 4.0 总体：修复回路从二态到三态

现有流水线的状态机：`UNSAT → 修复；SAT → 接受`。
SPARC 的状态机：

```
           ┌────────────── UNSAT ──────────────┐
           │   core-guided repair（继承，机制3约束）  │
           ▼                                    │
  翻译 T(P) → C, cov ──求解──┤
           │                                    │
           │        SAT ∧ |Sol|>1（π-gate 检出欠约束）
           │                ▼
           │     diff-guided completion（机制2，机制3约束）
           │                │
           └── SAT ∧ 唯一 ∧ 良定 → 接受并作答
                    （预算耗尽 → 弃答，输出"无可信解"）
```

三态修复的每一轮都带预算；终止语义是 selective prediction：
只有通过 π-gate 的答案才输出。

### 4.1 机制 1：结构先验门控（π-gate）

**zebra 实例化（唯一性检查）**
```
输入: C (SAT), 模型 M
1. C' ← C ∪ { ¬(⋀_{v∈vars(M)} v = M[v]) }     # 阻塞子句
2. r ← SAT?(C')
3. r = UNSAT → 唯一 → PASS
   r = SAT   → 取第二模型 M'，返回 (FAIL_UNDER, M, M')   # 进入机制 2
   r = UNKNOWN → 按超时策略降级为 PASS-with-flag（记录，不作答影响待消融）
```
成本：每次接受多一次求解调用（zebra 规模下 <0.2s）。

**AR-LSAT 实例化（良定性检查）**
选项谓词 {φ_A..φ_E} 在 C 上的判定向量必须与题型的良定模式一致：
could-be-true 题要求 **恰一个** φ_k 满足 SAT(C ∧ φ_k)；must-be-true 题要求
恰一个 φ_k 满足 UNSAT(C ∧ ¬φ_k)；EXCEPT 镜像。
判定向量与模式不符（0 个或 ≥2 个候选）⇒ FAIL_UNDER 或 FAIL_OVER
（0 候选多为过约束或选项翻译缺陷；≥2 候选为欠约束——分流进不同修复）。
注：这把我们已验证的轨迹判定逻辑（judge()）从挖掘期前移到测试时。

**误杀分析（需在论文中报告）**：π-gate 唯一无法检出的 SBW 是
"误约束型"（唯一但错）。RQ2 实验用真值离线核算其占比；
可见性论证预言互锁充分时该占比很小（zebra baseline 实测 0/640）。

### 4.2 机制 2：模型差分归因（diff-guided completion）

**输入**：欠约束证据 (M, M')，覆盖账本 cov，线索集 L。

```
1. D ← { v : M[v] ≠ M'[v] }                    # 差异变量集
2. L_D ← { l ∈ L : mentions(l, D) }             # 提及差异变量的线索（词面/实体匹配）
3. 缺陷候选排序：
   a. L 中无覆盖的线索（cov 中无对应约束）且 ∈ L_D   —— 漏译，最高优先
   b. L_D 中覆盖约束为弱形式的线索                    —— 弱译（如 < 译成 ≠）
   c. 其余 L_D                                       —— 语义偏差候选
4. 修复 prompt（一次一条线索）：
   给 LLM：原线索文本 + 该线索现有约束（或"无"）+ D 的两组取值对照
   要求：输出能区分 M 与 M' 的更强约束（必须使二者至少其一违背）
5. 验证接受：新约束 c* 需满足
   a. SAT(C ∪ {c*})                              # 不引入矛盾
   b. ¬(M ⊨ c* ∧ M' ⊨ c*)                        # 真的区分了两个模型（进展保证）
   c. 机制 3 不变量
6. 回到求解 → π-gate；每轮至多 k_diff 次（预算）
```

**进展保证（可证明的小命题）**：步骤 5b 确保每轮接受的约束至少排除一个
现存模型，解空间严格缩小 ⇒ 欠约束修复在 |Sol(C)| 上单调收敛，
不会像 SAT-seeking 修复那样打转。这是论文里为数不多可形式陈述的性质，值得写成命题。

**与 UNSAT core 的对称性（写作卖点）**：
core = "这些约束合谋导致无解"（删/改的候选）；
diff = "这些变量在多解间摇摆"（补/强化的候选）。
一收一放，构成完整的双向诊断。

### 4.3 机制 3：可见性保全不变量（no-weakening invariant）

修复动作 a: C → C' 的准入规则（三条，全部机械可查）：

1. **覆盖不降**：∀l ∈ L，cov 中 l 的关联约束数在 C' 中不少于 C
   （允许替换，禁止裸删除；删除必须伴随同线索的替代翻译）；
2. **解空间单调性（按修复方向）**：
   欠约束修复要求 |Sol(C')| < |Sol(C)|（由 4.2-5b 保证）；
   过约束修复允许放松，但**只允许改动 UNSAT core 内约束**，
   且改后需对该线索做再对齐检查（LLM 判断新约束是否仍忠实于线索原文——
   一次廉价调用，或用词面规则近似）；
3. **SAT 后必过 π-gate**：任何修复达成的 SAT 不再直接接受（4.0 状态机）。

这三条合起来堵死 SBW 通道：规则 1/2 阻止互锁被侵蚀（错误保持可见），
规则 3 兜底探测残余的不可见错误。原 PRISM 的 L1–L4 升级阶梯废除；
如保留温和的目标切换（L1），必须在不变量约束下运行。

### 4.4 覆盖账本（cov）的获得

翻译 prompt 增加一列输出：每条约束前缀 `# clue_i`。
解析后建立 cov；无标注的约束归入 `clue_0`（背景/域约束）。
账本质量本身可度量（漏标率），并在消融中检验其对机制 2 定位命中率的影响。
（工程注记：assert_and_track 的追踪名可直接编码线索号，无新增求解器机制。）

### 4.5 实验设计骨架（对应 RQ，不在本文档展开）

- **主表**：zebra 官方 640 × {Logic-LM 式 baseline, baseline+SPARC,
  积极修复(nopar), 积极修复+SPARC} × 3 种子 × GPT-4o-mini；GPT-4o 对照行。
- **SBW 核算表**：各系统 SAT且对 / SAT但错 / 弃答 三列（论文 A 的签名表格）。
- **风险-覆盖曲线**：π-gate 弃答语义下的 selective prediction 视角。
- **消融**：−π-gate / −模型差分（检出后盲重译）/ −不变量 / −覆盖账本（差分不定位）。
- **RQ2/RQ3 度量表**：检出查准查全、定位命中率、补全成功轮次。
- **AR-LSAT**：良定性实例化重复主表（RQ5）。

---

## 五、AAAI 创新性层次评估

| 层次 | 论文 A 的对应物 | 强度 |
|---|---|---|
| 新现象/新问题 | SBW 坍缩：量化（54–60/640 精确核算 + 77% 轨迹）+ 机制归因（可见性论证） | **强**（文献只有零散观察，无量化、无归因、无名字） |
| 新原理/视角 | 错误可见性；结构先验作为免费正确性代理；弃答的形式语义 | **中强** |
| 新技术 | 双向诊断（core ↔ 模型差分）+ 进展保证 + 保全不变量 + 覆盖账本 | **中**（组合价值 > 单件价值；模型差分→线索归因与进展命题是最硬的新件） |
| 实证 | 两基准 × 两模型 × 逐机制消融 + 失败类型学核算 + 风险-覆盖分析 | 强（基础设施就绪） |

**预答辩**：
- "唯一性检查是 folklore" → 单件确实；贡献在完整信号体系 + 差分归因 +
  进展保证 + SBW 诊断证明其必要性。写作上机制 2/3 当技术主角。
- "与 VERGE 区别" → MCS 在过约束侧，我们补欠约束侧，正交（专段对照）。
- "只在弱模型上有效？" → GPT-4o 对照行如实呈现；SBW 是修复回路的结构性质，
  强模型修复少但一旦修复同样面临信号错位（且 π-gate 成本近零，无害保险）。
- **增益不确定性（诚实预估）**：zebra baseline 的 wrong-SAT 已为 0，π-gate
  对它无增益；论文的胜负手在 RQ4——"积极修复 + SPARC ≥ baseline 且 SBW≈0"。
  若只得安全性无正增益，退守"诊断 + selective prediction"叙事（TMLR 备选）。

**达标条件**：机制 2/3 实现质量 + 至少一个基准显著正增益 ⇒
AAAI "solid technical contribution + strong analysis" 档位。

---

## 六、范围纪律（这篇论文不做什么）

**做**：SBW 诊断；三机制 + 状态机；zebra（640 官方）+ AR-LSAT（230）；
GPT-4o-mini 主表 + GPT-4o 对照行；逐机制消融；失败类型学与风险-覆盖分析。

**不做**（每一条都是抵抗过的诱惑）：
- ✂️ 范式库/跨题记忆（下一篇：难目标语言的编码惯用法记忆——PDDL/Lean/CP）
- ✂️ ASP/其他表示语言
- ✂️ K&K、LogicalDeduction 基准
- ✂️ 修复轨迹记忆作为贡献（只作 §2 动机证据）
- ✂️ 模型计数/概率推断的深水区（只用第二模型查询）
- ✂️ 多于两行的多模型缩放
- ✂️ 定理证明/程序合成的推广（结论一句话）
